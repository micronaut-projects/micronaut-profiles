description: Configures SQL DataSource instances using Tomcat Connection Pool
dependencies:
  - scope: compile
    coords: io.micronaut.configuration:micronaut-jdbc-tomcat
  - scope: runtime
    coords: com.h2database:h2