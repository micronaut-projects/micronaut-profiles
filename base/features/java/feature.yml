description: Adds support for Java in the application
build:
    plugins:
        - java
dependencies:
    compile:
        - io.micronaut:inject
        - io.micronaut:runtime
    testCompile:
        - junit:junit:4.12