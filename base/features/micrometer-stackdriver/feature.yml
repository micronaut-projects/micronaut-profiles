description: Adds support for Micrometer metrics (w/ Stackdriver reporter)
features:
    dependent:
      - management
dependencies:
    - scope: implementation
      coords: io.micronaut.configuration:micronaut-micrometer-core
    - scope: implementation
      coords: io.micronaut.configuration:micronaut-micrometer-registry-stackdriver