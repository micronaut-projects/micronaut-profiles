description: Adds support for Micrometer metrics (w/ Jmx reporter)
features:
    dependent:
      - management
dependencies:
    - scope: compile
      coords: io.micronaut.configuration:micronaut-micrometer-core
    - scope: compile
      coords: io.micronaut.configuration:micronaut-micrometer-registry-jmx