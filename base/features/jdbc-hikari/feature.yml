description: Configures SQL DataSource instances using Hikari Connection Pool
dependencies:
    compile:
        - io.micronaut.configuration:jdbc-hikari
    runtime:
        - com.h2database:h2:1.4.196