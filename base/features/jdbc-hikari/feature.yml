description: Configures SQL DataSource instances using Hikari Connection Pool
dependencies:
  - scope: compile
    coords: io.micronaut.configuration:micronaut-jdbc-hikari
  - scope: runtime
    coords: com.h2database:h2