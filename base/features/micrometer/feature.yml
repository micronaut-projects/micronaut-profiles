description: Adds support for Micrometer metrics
features:
    dependent:
      - management
dependencies:
    - scope: implementation
      coords: io.micronaut.configuration:micronaut-micrometer-core