description: Configures GORM for MongoDB for Groovy applications
features:
    dependent:
      - groovy
      - mongo-reactive
dependencies:
  - scope: implementation
    coords: io.micronaut.configuration:micronaut-mongo-gorm

        