description: Configures GORM for MongoDB for Groovy applications
features:
    dependent:
      - groovy
      - mongo-reactive
dependencies:
  - scope: compile
    coords: io.micronaut.configuration:mongo-gorm

        