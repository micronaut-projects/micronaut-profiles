description: Adds support for the Spek testing framework
dependentFeatures:
    - kotlin
dependencies:
  - scope: testCompile
    coords: org.jetbrains.kotlin:kotlin-test::${kotlinVersion}
  - scope: testCompile
    coords: org.spekframework.spek2:spek-dsl-jvm:${spekVersion}
  - scope: testRuntime
    coords: org.spekframework.spek2:spek-runner-junit5:${spekVersion}