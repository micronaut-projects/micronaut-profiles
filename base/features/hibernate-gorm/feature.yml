description: Adds support for GORM persistence framework
features:
    dependent:
      - groovy
dependencies:
  - scope: compile
    coords: io.micronaut.configuration:micronaut-hibernate-validator
  - scope: compile
    coords: io.micronaut.configuration:micronaut-hibernate-gorm
  - scope: runtime
    coords: com.h2database:h2
  - scope: runtime
    coords: org.apache.tomcat:tomcat-jdbc