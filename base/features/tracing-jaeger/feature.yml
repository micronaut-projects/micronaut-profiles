description: Adds support for distributed tracing with Jaeger (https://www.jaegertracing.io)
dependencies:
  - scope: implementation
    coords: io.micronaut:micronaut-tracing
  - scope: runtimeOnly
    coords: io.jaegertracing:jaeger-thrift
