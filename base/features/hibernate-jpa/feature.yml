description: Adds support for Hibernate/JPA
dependencies:
  - scope: compile
    coords: io.micronaut.configuration:micronaut-hibernate-jpa
  - scope: compile
    coords: io.micronaut.configuration:micronaut-jdbc-tomcat
  - scope: runtime
    coords: com.h2database:h2    
        
