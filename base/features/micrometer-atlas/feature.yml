description: Adds support for Micrometer metrics (w/ Atlas reporter)
features:
    dependent:
      - management
dependencies:
    - scope: compile
      coords: io.micronaut.configuration:micrometer-core
    - scope: compile
      coords: io.micronaut.configuration:micrometer-registry-atlas